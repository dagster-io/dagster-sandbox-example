locations:
  sandbox-example:
    build: .
    base_image: dagster/dagster-cloud-base
    registry: dagster/dagster-sandbox-example
    package_name: example
    code_sync:
      - from: example
        to: /opt/dagster/app/example
